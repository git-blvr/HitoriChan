Replace embeds with Discord Components v2 and add cv2 helper.

All command output that used EmbedBuilder now uses Components v2 via the
new `src/helpers/cv2.js` helper. The helper turns a simple options object
(title, description, fields, footer, images, action rows, etc.) into a
proper v2 message payload with a Container and the IsComponentsV2 flag.

- New `cv2()` helper for declarative v2 container building.
- Updated `buildEmbed`, `createBasicEmbed`, `createDescBasicEmbed` to use cv2.
- Converted help, moderation, economy, games, ai, server, tools, and streak
  outputs to v2.
- Help menu now embeds action rows inside a v2 container.
- Verified all commands still load before the dummy-token login step.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
