Add web dashboard with login, logs, triggers, and config pages.

- New Express web server in `src/web/` starting on `WEB_PORT` (default 3000).
- Login with `Admin` and a random 6-digit password printed to console.
- 24-hour JWT session stored in an HTTP-only cookie.
- Dashboard layout: left navigation, top header, main content area.
- Pages for Economy, AI, Streak, Moderation, Prefix, Triggers, and Logs.
- New SQLite tables: `command_logs`, `message_logs`, `triggers`.
- Log every command and message to the database.
- Trigger handler that runs a prefix command when a keyword is seen.
- Updated `index.js` to start the dashboard after Discord login.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
